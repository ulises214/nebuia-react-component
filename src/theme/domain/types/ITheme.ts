export type Theme = {
  primary: string;
  secondary: string;
  text: string;
  textSecondary: string;
  background: string;
  secondaryBackground: string;
  dark: boolean;
  primaryText: string;
  secondaryText: string;
};
